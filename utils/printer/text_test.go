package printer

/*
func main() {
	content := `A text input field, akin to an <input type="text"> in HTML. Supports unicode, pasting, in-place scrolling when the value exceeds the width of the element and the common, and many customization options.`
	res := NewText(content, "input", "value").
		SetStyle(lipgloss.
			NewStyle().
			Foreground(lipgloss.
				Color("12"))).
		String()
	fmt.Println(res)
}
*/
